"""Adversarial and AI-safety tests (02_ARCHITECTURE.md §7.3, TASK-113).

These are the tests that distinguish "looks done" from "is actually
trustworthy". 05_SECURITY.md §10.11 makes them a release gate: a build that has
not run all five has not met this application's security bar, regardless of how
complete its features otherwise are.
"""
